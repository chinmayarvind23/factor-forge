[project]
name = "factorforge-training"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "factorforge",
  "trl==1.13.0",
  "torch>=2.6,<3",
  "peft>=0.18,<1",
]

[tool.uv.sources]
factorforge = { path = "../..", editable = true }
