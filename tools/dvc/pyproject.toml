[project]
name = "factorforge-dvc-tooling"
version = "0.1.0"
description = "Isolated DVC tooling with a required dependency-security gate"
requires-python = ">=3.12"
dependencies = [
  "dvc[s3]==3.67.1",
  "pip-audit==2.10.1",
]

[tool.uv]
package = false
