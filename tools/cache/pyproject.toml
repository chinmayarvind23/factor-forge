[project]
name = "factorforge-cache"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["factorforge", "redis>=6,<9"]

[tool.uv.sources]
factorforge = { path = "../..", editable = true }
