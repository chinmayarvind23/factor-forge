[project]
name = "factorforge-planning"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["factorforge", "deepagents==0.7.13", "pip-audit>=2.10.1,<3"]

[tool.uv.sources]
factorforge = { path = "../..", editable = true }
